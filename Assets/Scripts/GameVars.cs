using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameVars : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameGlobals.levelLoaded = true;
    }
}
